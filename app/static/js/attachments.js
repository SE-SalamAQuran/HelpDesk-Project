document.addEventListener("DOMContentLoaded",()=>{const f=document.getElementById("attachmentForm");if(!f)return;f.onsubmit=async e=>{e.preventDefault();if(!attachmentFile.files.length)return toast("Choose a file","error");const fd=new FormData();fd.append("file",attachmentFile.files[0]);try{const d=await API.request(APP_CONFIG.ENDPOINTS.uploadAttachment(CURRENT_TICKET_ID),{method:"POST",body:fd});attachmentResult.innerHTML=`<b>Uploaded</b><br>${esc(d.file_name||attachmentFile.files[0].name)}<br><small>${esc(d.file_path||"")}</small>`;attachmentFile.value="";toast("Attachment uploaded")}catch(x){toast(x.message,"error")}}});