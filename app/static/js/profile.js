document.addEventListener("DOMContentLoaded",async()=>{if(!requireAuth())return;try{const p=await API.request(APP_CONFIG.ENDPOINTS.profile),n=p.name||p.email||"User";profileName.textContent=n;profileEmail.textContent=p.email||"—";profileRole.textContent=p.role||"employee";profileAvatar.textContent=n[0].toUpperCase();verified.textContent=p.email_verified?"Email verified":"Email not verified";permissions.innerHTML=(p.permissions||[]).map(x=>`<span class="permission">${esc(x)}</span>`).join("")||'<span class="muted">No permissions</span>'}catch(e){toast(e.message,"error")}});